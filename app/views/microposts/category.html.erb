<% if @category_microposts.blank?%>
<p>『<%= @category.name %>』の投稿はまだありません。</p>

<% else %>
<p class="">『<%= @category.name %>』の投稿・<%= @category_microposts.count %>件</p>
<div class="pict-wrapper">
  <% @category_microposts.each do |micropost| %>
    <div class="pict shadow bg-white rounded p-1 m-1">
      <%= link_to image_tag(micropost.picture.url), micropost_path(micropost.id)  if micropost.picture? %>
    </div>
  <% end %>
</div>
<%= paginate @category_microposts %>

<% end %>