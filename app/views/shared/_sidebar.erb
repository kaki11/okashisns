<div class="side-contents p-2">
  <p class="category">CATEGORY</p>

  <span class="categorys">Sweets</span>
  <% @sweets_categories.each do |c| %>
    <div class="category-list">
      <%= link_to c.name, "#" %>
    </div>
  <% end%>


  <p class="category"></p>

  <span class="categorys">Bread</span>
  <% @bread_categories.each do |c| %>
    <div class="category-list">
      <%= link_to c.name, "#" %>
    </div>
  <% end%>

  <p class="category"></p>

  <span class="categorys">Other</span>
  <% @other_categories.each do |c| %>
    <div class="category-list">
      <%= link_to c.name, "#" %>
    </div>
  <% end%>
  
</div>